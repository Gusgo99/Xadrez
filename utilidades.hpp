#ifndef UTILIDADES_HPP
#define UTILIZADES_HPP

#define abs(var) ((var > 0) ? (var) : -(var))
#define maior(a, b) ((a) > (b) ? (a) : (b))
#define menor(a, b) ((a) > (b) ? (b) : (a))
#define norma(a, b) (sqrt((a * a) + (b * b)))

#endif
