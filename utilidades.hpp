#ifndef UTILIDADES_HPP
#define UTILIZADES_HPP

// Comentar para iniciar jogo em modo normal:
//#define DEBUG

// Retorna o valor absoluto
#define abs(var) ((var > 0) ? (var) : -(var))

// Retorna o maior valor entre os dois argumentos
#define maior(a, b) ((a) > (b) ? (a) : (b))

// Retorna o menor valor entre os dois argumentos
#define menor(a, b) ((a) > (b) ? (b) : (a))

#endif
